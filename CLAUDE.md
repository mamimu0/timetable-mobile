# プロジェクトの目標

- 時間割（index.html / timetable.js）をスマホのロック画面（ウィジェット・壁紙・通知など）で確認できるようにすることが目標です。
- 将棋関連の目標・作業はこのプロジェクトには含まれません。

# 実行ルールと制約

- **動作範囲:** このプロジェクトフォルダ（現在地）内のみで作業してください。
- **範囲外アクセス禁止:** 親ディレクトリ（`..`）やホームディレクトリへのアクセス、ファイル参照・変更は禁止します。
- **禁止操作:** `.env` や機密情報が含まれるファイル、`node_modules` などの大容量フォルダの全件読み込みは行わないでください。
- **破壊的変更の確認:** ファイルの削除（rm）や設定の変更を行う前に、必ずユーザーへ確認を取ってください。
